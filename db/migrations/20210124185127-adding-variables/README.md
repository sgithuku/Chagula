# Migration `20210124185127-adding-variables`

This migration has been generated by Shaun at 1/24/2021, 6:51:27 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Meal" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "name" TEXT,
    "category" TEXT,
    "selected" BOOLEAN NOT NULL DEFAULT false,
    "timesEaten" INTEGER NOT NULL DEFAULT 0,
    "image_url" TEXT,
    "already_eaten" BOOLEAN NOT NULL DEFAULT false
);
INSERT INTO "new_Meal" ("id", "createdAt", "updatedAt", "name", "category", "selected") SELECT "id", "createdAt", "updatedAt", "name", "category", "selected" FROM "Meal";
DROP TABLE "Meal";
ALTER TABLE "new_Meal" RENAME TO "Meal";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210122174737-selected-column..20210124185127-adding-variables
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -37,15 +37,18 @@
   privateData        String?
 }
 model Meal {
-  id        Int      @id @default(autoincrement())
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  name      String?
-  category  String?
-  days      Day[]
-  selected  Boolean  @default(value: false)
+  id            Int      @id @default(autoincrement())
+  createdAt     DateTime @default(now())
+  updatedAt     DateTime @updatedAt
+  name          String?
+  category      String?
+  days          Day[]
+  selected      Boolean  @default(value: false)
+  timesEaten    Int      @default(value: 0)
+  image_url     String?
+  already_eaten Boolean  @default(value: false)
 }
 model Day {
   id        Int      @id @default(autoincrement())
```


